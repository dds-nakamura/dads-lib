/**
 * Vitest セットアップファイル。
 *
 * Phase 2 時点ではプレースホルダ。Phase 6 で vitest-axe の matchers 登録を
 * 追加予定:
 *
 *   import * as matchers from 'vitest-axe/matchers'
 *   import 'vitest-axe/extend-expect'
 *   expect.extend(matchers)
 *
 * @vue/test-utils v2.4+ は `@vue/test-utils` の自動 cleanup を行うため、
 * afterEach での explicit な unmount は不要。
 */

export {}
