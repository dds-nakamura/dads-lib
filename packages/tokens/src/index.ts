/**
 * @dads/tokens — DADS デザイントークンの再エクスポート。
 *
 * 公式 npm パッケージ @digital-go-jp/design-tokens の JS module をそのまま
 * 再エクスポートする。CSS は `@dads/tokens/css` または `@dads/tokens/css-simple`
 * でアクセスする。
 *
 * 使用例:
 *   import '@dads/tokens/css'             // CSS 変数を読み込む
 *   import { Color } from '@dads/tokens'  // JS から型付きで参照
 */

export * from '@digital-go-jp/design-tokens'
